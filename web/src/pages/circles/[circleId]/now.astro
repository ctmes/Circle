---
import Base from "../../../layouts/Base.astro";
import { OverviewView } from "../../../components/OverviewView";

export const prerender = false;
const { circleId } = Astro.params;
---
<Base title="Now">
  <OverviewView client:load circleId={circleId!} />
</Base>
