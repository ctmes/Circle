---
import Base from "../../../layouts/Base.astro";
import { MainView } from "../../../components/MainView";

export const prerender = false;
const { circleId } = Astro.params;
---
<Base title="Main">
  <MainView client:load circleId={circleId!} />
</Base>
