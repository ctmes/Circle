---
import Base from "../../../layouts/Base.astro";
import { RecordView } from "../../../components/RecordView";

export const prerender = false;
const { circleId } = Astro.params;
---
<Base title="Claims">
  <RecordView client:load circleId={circleId!} initialKind="claims" />
</Base>
